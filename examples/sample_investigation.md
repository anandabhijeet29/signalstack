## Investigation Log — 2026-05-12

> *Generated by `signalstack run --investigate`. The agent autonomously decided what was
> worth investigating, used tools to find evidence, and wrote this log explaining every
> decision.*

---

### Thread: "Compute scaling hitting an efficiency wall"

**Trigger:** Articles #2 (Latent Space) and #4 (Stratechery) both describe diminishing
returns on raw compute scaling — but reach opposite conclusions about what this means
for AI progress. The contradiction is worth resolving.

**Decision:** Search for recent research on whether efficiency gains from architecture
improvements are outpacing raw compute scaling, which would settle the contradiction
between these two takes.

**Action:** Searched for "compute scaling efficiency wall 2026 architecture improvements research"

**Found:** Two relevant papers from April 2026. The first (DeepMind) argues that
mixture-of-experts architectures achieve GPT-4 performance at 40% of the compute cost.
The second (Anthropic) shows inference-time compute is becoming the new scaling lever,
separate from training compute entirely.

**Key finding:** The Latent Space and Stratechery pieces are talking about different
things without realizing it. Latent Space is right that *training* compute scaling
is hitting diminishing returns. Stratechery is right that *total* AI capability is
still accelerating — because inference-time compute and architectural efficiency are
filling the gap. The "wall" narrative is technically correct but strategically misleading.

**Connection:** This reframes Article #1 (Noahpinion on AI economic impact). If
inference-time compute is the new scaling frontier, economic impact arrives faster
than training-compute timelines suggest — because inference is deployable immediately,
not in 3-year training cycles.

---

### Thread: "Open-source model convergence with frontier models"

**Trigger:** Three separate articles this week mention open-source models reaching
"GPT-4 level" performance. This claim has been made prematurely before. Is this time
different?

**Decision:** Fetch the LMSYS Chatbot Arena leaderboard to check whether open-source
models have actually closed the gap on hard reasoning benchmarks — the previous claims
were based on cherry-picked benchmarks.

**Action:** Fetched `https://lmsys.org/blog/2024-05-13-arena/` and cross-referenced
with find_related("open source model benchmark")

**Found:** The leaderboard shows Llama 4 Scout at Elo 1287 vs. GPT-4o at 1285 on
general tasks. However, on the "hard prompts" subset (math, coding, multi-step
reasoning), GPT-4o leads by 47 Elo points — a meaningful gap.

**Key finding:** The convergence is real for general tasks but the gap persists on
hard reasoning. The "GPT-4 level" claim is accurate for the median user's use case
but misleading for developers building agents or technical applications. The three
articles in this digest are implicitly making different claims depending on their
audience — none are wrong, but none are complete.

**Connection:** This directly affects Article #3's thesis (The Zvi) that open-source
models enable a wave of cheap AI agents. For *simple* agent tasks, yes. For *complex*
multi-step reasoning agents, the frontier models still have a meaningful moat.

---

### Summary

Two significant findings this week. First, the "compute scaling wall" debate is a
definitional conflict, not a factual one — training compute is plateauing while
inference compute and architecture efficiency are accelerating. Second, open-source
model convergence is real for general tasks but not for hard reasoning, which changes
the strategic picture for anyone building agents. Both findings are connections the
individual articles don't make on their own.
